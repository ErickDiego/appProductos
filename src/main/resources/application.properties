#spring.application.name=appProductos
# swagger-ui custom path
springdoc.swagger-ui.path=/swagger-ui.html

#Habilitar o Deshabilitar Swagger
springdoc.swagger-ui.enabled=true

server.port=8080

# Configuracion de la base de datos H2 en memoria
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
# Habilitar la consola de H2
spring.h2.console.enabled=true

# DDL auto: create para que cree automaticamente las tablas segun las entidades
spring.jpa.hibernate.ddl-auto=create

# Modo de inicializacion de la base de datos
spring.datasource.initialization-mode=always

#logging.level.org.springframework=DEBUG